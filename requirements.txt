# extrair_bkp.py, bf_tabela.py e extrair_tabela.py usam apenas a biblioteca
# padrão (struct, zlib, re, csv). As dependências abaixo são para o resto:

# consolidar.py — join entre as tabelas
pandas>=2.0

# gerar_marc.py e inserir_exemplares.py — MARC21/ISO 2709 (ver docs/ROADMAP.md)
pymarc>=5.0

# inserir_exemplares.py — insere os exemplares direto no banco do BibLivre.
# O instalador do BibLivre traz o PostgreSQL, mas não expõe o psql no PATH,
# por isso o passo é feito por aqui e não por linha de comando do psql.
psycopg2-binary>=2.9

# detectar_ficha.py — detecção e retificação da ficha CIP via OpenCV
opencv-python>=4.8
pytesseract>=0.3.10

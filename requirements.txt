# Monorepo: cada pacote declara as próprias dependências no seu pyproject.toml.
# Instalar em modo editável é o que faz `from biblio.biblivre import obras`
# funcionar de qualquer lugar, sem sys.path.insert em script nenhum.
#
#   pip install -r requirements.txt
#
# Ordem importa: os de baixo dependem dos de cima.

-e ./packages/bf-legado
-e ./packages/biblivre-client
-e ./packages/catalogacao
-e ./apps/api

# OCR da ficha CIP (livro sem código de barras). Opcional: quem só usa o
# leitor de código de barras não precisa de OpenCV nem Tesseract — o endpoint
# /api/capturar responde 501 em vez de derrubar o servidor.
opencv-python>=4.8
pytesseract>=0.3.10
pillow>=10.0

# Migração de acervo legado (leitura do .bkp do Biblioteca Fácil).
pandas>=2.0

# Reload do servidor em desenvolvimento (`scripts/dev.py`). Sem watchfiles o
# uvicorn cai no StatReload, que faz polling de mtime e ignora os filtros de
# arquivo — funciona, mas reinicia mais devagar e observa mais do que precisa.
watchfiles>=0.21
